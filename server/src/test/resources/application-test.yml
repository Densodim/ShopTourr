spring:
  datasource:
    url: jdbc:h2:mem:voyage;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

voyage:
  jwt:
    secret: test-hmac-secret-key-must-be-32-bytes!
